package snownee.loquat.duck;

import snownee.loquat.core.AreaManager;

public interface AreaManagerContainer {

    AreaManager loquat$getAreaManager();

    void loquat$setAreaManager(AreaManager var1);
}